using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace H264Images
{
    public class BuildImage
    {
        /*public Bitmap BuildImageFromList(Bitmap bmp1, List<H264Images.SetListClass.PixelsDetails> list)
        {  
            Size s1 = bmp1.Size;
            
            Bitmap bmpNew = new Bitmap(s1.Width, s1.Height);

            for (int y = 0; y < s1.Height; y++)
                for (int x = 0; x < s1.Width; x++)
                {
                    Console.WriteLine(list);
                    //if (list.[x] == x && list[y] == y) 
                    //bmp3.SetPixel()
                    //Color c1 = bmp1.GetPixel(x, y);

                    //if (c1 == c2) bmp3.SetPixel(x, y, c1);
                    //else bmp3.SetPixel(x, y, diffColor);
                }
            return bmpNew;
        }*/
    }
}
